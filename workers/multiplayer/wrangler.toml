name = "coldwar-multiplayer"
main = "src/index.ts"
compatibility_date = "2025-04-01"
workers_dev = true

# Custom domain route
routes = [
  { pattern = "coldwar-multiplayer.183107.xyz/*", zone_id = "6a337c1f263ad6b69f56acb8429dc36a" },
]

# Durable Objects binding
[[durable_objects.bindings]]
name = "GAME_ROOM"
class_name = "GameRoom"

# Durable Object migration (new class, SQLite storage for free plan)
[[migrations]]
tag = "v1"
new_sqlite_classes = ["GameRoom"]
